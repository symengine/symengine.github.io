# API Exploration
As befitting a library with bindings to multiple languages, the documentation can be a little confusing. Here, we explore the core concepts of the API from a user's point of view. The language bindings do not depend on each other, and the same tasks are explored in each instance through interactive notebooks.

```{toctree}
:maxdepth: 2
C++ Usage <cpp/index>
Python Usage <py/index>
```
